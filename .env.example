# Copy to .env and fill in your keys. .env is git-ignored — never commit real keys.
OPENAI_API_KEY=sk-...          # gpt-5.4-mini (classify/refine) + gpt-5.5 (gate/extract/resolve/dedup/demographics)
llamaparse_api_key=llx-...     # LlamaParse OCR (stage 4)
